var RangeSelector = pc.createScript('rangeSelector');

// initialize code called once per entity
RangeSelector.prototype.initialize = function() {
    
};

// update code called every frame
RangeSelector.prototype.update = function(dt) {
    
};